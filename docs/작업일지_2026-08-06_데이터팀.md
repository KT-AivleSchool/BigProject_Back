# 작업일지 — 2026-08-06 (데이터팀 / 김규민)

브랜치: `poc/statute-ingest` · 대상 DB: `omnisite` (PostGIS 3.3.4 + pgvector)

## 요약
연속지적도 적재, DB seed 자동복원 셋업, Redis 영속화, 코드체계 조인 확정까지 완료.
`docker compose up`만으로 팀원이 데이터를 받는 구조를 만들었고, 감리팀(popzap) 회신 3건을 실측 검증했다.

---

## 1. 연속지적도(LSMD) 적재 — 신규
- **결과**: `cadastral_lands` **44,459필지**(용산) 적재. geom_5186 자동생성, invalid 0.
- **산출**: `schema_cadastral.sql`(DDL) + `scripts/load_cadastral.py`(로더, 멱등 가드).
- **설계**: 원본 SHP(5186) 컬럼(PNU 19·JIBUN·COL_ADM_SE) 보존 + geom(4326)+geom_5186 규약. PNU는 문자열(앞자리 0 보존).
- **검증**: popzap 실측값(44,459 / 법정동 36 / PNU 문자열)과 **100% 일치**.

## 2. DB seed 자동복원 셋업 — 신규 (팀 공유 핵심)
- **`omnisite_seed.sql.gz` (9.3MB)** 생성 — `pg_dump` + gzip. 경계 3종(640MB)·spatial_ref_sys 데이터 제외.
- **`docker-compose.yml`**에 `/docker-entrypoint-initdb.d/` 마운트 추가 → **빈 볼륨 최초 기동 시 자동 복원**(기존 데이터 있으면 미실행 = 안전).
- **버림 컨테이너 복원 테스트 통과**(에러 0): cadastral 44,459·상가 15,722·크로스워크 3,555·조례 222 등 복원 확인.
- 효과: 팀원이 `git pull → docker compose up`으로 전부 자동 수령(`.env`만 별도).

## 3. Redis 영속화 — `docker-compose.yml` 수정 + 적용
- **AOF(everysec) + RDB + 볼륨 + `volatile-lru`** 4세트 적용·검증(AOF 파일 볼륨 생성 확인).
- 목적: 지오코딩·지목·LLM 캐시가 재시작·컨테이너 삭제에도 보존.
- **popzap 지적 수용**: `volatile-lru`는 evict 압력을 보안키(잠금·레이트리밋)로 옮김 → 현재 무해(TTL키만 존재), (c) 현행유지+모니터링으로 합의.

## 4. 코드체계 조인 확정 (#205/#208) — 검증 완료
- **정제본=통계청, topN=법정동(PNU앞10), 크로스워크로 topN 조인 불가** 확정.
- 실측: 공유지 2,486점 `ST_Within`으로 경계 매칭 2,486/2,486. topN은 공간조인(ST_Within)으로 붙이는 것으로 합의.
- **`schema_region_boundaries.sql` 주석 보강**: "직접 조인 시 7/16 조용한 부분매칭" 경고 + topN ST_Within 패턴 명시.

## 5. 기타
- **`requirements.txt`에 `pyarrow` 추가**(#208) — 없으면 parquet→csv 폴백, 행정동코드 앞자리 0 유실. 로컬 설치+검증.
- **`booth_candidates` 더미 1행** 삽입(테스트용, score 0.87/rank 1, 용산 좌표) + seed 재생성 반영.
- **`docs/데이터팀_DB_구조_가이드.md`** 작성 — 파일 인벤토리·DB 흐름·온보딩.
- **`omnisite_backup.sql`(7/14 stale, 19MB) 삭제 예정** — seed가 대체.

## 6. 조사·확인 (적재 불필요 판정)
- **조례 RAG**: 이미 적재됨(222청크, 흡연 178+EV 44, 구조화 조문 메타). 재활용 0 = 정상(이격규정 없음). → 추가 적재 불필요.
- **audit_rule**: 그런 테이블 없음. 감리 규칙은 코드(`classifier.py`), 입력은 런타임 시뮬결과. `verified_precedents` 0행 = 정상(런타임 생성).
- **facility_type**: 버스/지하철은 참조 POI라 facility_type 아님 — 실제로도 별도 테이블(컬럼 없음), 정상.
- **VWORLD_KEY**: 용산은 좌표 이미 있어 불필요. 성동구 확장 시 필요(키 발급 선행).

---

## 커밋 대기 파일
```
M  docker-compose.yml            (Redis 영속화 + DB 자동복원)
M  requirements.txt              (pyarrow)
M  schema_region_boundaries.sql  (코드체계 조인 주석)
?? omnisite_seed.sql.gz           (9.3MB, booth 더미 포함)
?? schema_cadastral.sql
?? scripts/load_cadastral.py
?? docs/데이터팀_DB_구조_가이드.md
?? docs/작업일지_2026-08-06_데이터팀.md
─ git rm omnisite_backup.sql
```

## 이슈 대응
- **#205**(스키마 확인) 회신 검증 완료 — 코드체계 조인 확정.
- **#208**(설계·인수인계) 회신 검증 — volatile-lru 지적 수용, 연속지적도 스펙 일치.
- **#203**(산출물·Redis) 회신 검증 — ①②③④⑤⑥ 전부 합의/완료.

## 남은 것 (대기 / 선행조건)
- 커밋+푸시 (지금 로컬만) ← 최우선
- 팀 공지 (git pull → docker compose up)
- `invalidate(facility_name)` 패턴삭제 유틸 — Redis 캐시 이관 + 백엔드 인터페이스 확정 후
- 성동구 확장 — VWORLD 키 발급 + 지오코딩 선행
- topN 실제 조인 구현 — topN DB 서빙 시(방법=ST_Within 확정)
