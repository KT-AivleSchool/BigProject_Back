# 데이터팀 협의안(5개 항목) 검토 의견

> **목적:** AI 파이프라인팀 협의 문서(원본 데이터 요청·상호검증·STEP4 스펙·PostGIS 이관·DB 적재)에 대한 기술 검토 및 우선순위 정리.
> **근거:** 아래 "즉답" 표의 값은 현재 live DB(`omnisite-postgres-db`) 직접 조회 결과.
> **작성일:** 2026-07-30

---

## 0. 총평

문서 품질이 높음. 특히 두 지점이 정확하다.
- **"원본을 달라"** — 감리 AI가 프로파일(결측률·dtype·코드컬럼)로 op를 판정하는 구조라면, 정제본 입력 시 판정 근거가 소멸한다는 논리는 방법론적으로 타당.
- **"배제는 원본 union, 표출만 simplify"** — 표출 단순화가 법적 배제를 오염시키면 안 된다는 원칙과 일치. 정확한 설계.

이관·적재를 막거나 위험하게 만드는 **실질 이슈 4가지**만 회신에서 확정/합의하면 된다 → 6장 우선순위 표.

---

## 1. 그쪽 기술 질문에 대한 즉답 (live DB 실측)

| 질문 | 실측 결과 |
|---|---|
| PostGIS 3.1+ 필요 (`ST_SquareGrid`·`ST_MaximumInscribedCircle`) | ✅ **3.3.4**, 두 함수 존재 확인 → **업그레이드 불필요** |
| 대상 테이블 GiST 인덱스 | ✅ `candidate_lands`는 `geom`(4326)·`geom_5186`(5186) **둘 다 GIST** → 미터 기반 내접원 연산도 인덱스 활용 |
| 접근 권한(읽기/쓰기) | ⚠️ 로그인 role이 `postgres`(슈퍼유저) **하나뿐** → 최소권한 계정 부재 |
| 1계층 전국 경계 상시 적재됨? | ❌ **아직.** `dong_boundaries` 16행(용산 동만)·`districts` 1행 → **전국 경계 미적재** |
| 연속지적도 | `candidate_lands` 6,524행만 존재. **용산 44,459필지 원본 지적도는 DB에 없음**(후보만 필터 적재) |
| 스키마에 통계청/행자부 코드 주석 | ❌ **전무** |

---

## 2. 항목별 검토

### 1번 — 원본 데이터 요청 → **동의, 단 "샘플 원본"으로 범위 축소**
- 감리 AI가 보는 건 프로파일(컬럼·dtype·결측률·샘플값)이므로 **원본 스키마 + 상위 N행 샘플**이면 판정 경로 성립.
- 전량(생활인구 315k행 등)은 실제 적재 시점에만.
- **요청 문구를 "원본 스키마·샘플 제공"으로 좁히면** 데이터팀 부담 감소.

### 2번 — A vs B 상호검증 → **좋은 설계, 단 비교 기준 사전 합의 필요**
- 원본→AI정제(A) vs 원본→수작업(B) 대조는 유효한 QA.
- 성립 조건:
  - **조인 키**(행 매칭 컬럼)와 **허용 오차**(좌표 몇 m까지 "동일"로 볼지) 사전 정의
  - "차이"를 **행 누락 / 컬럼값 불일치 / 좌표 편차**로 분리 측정해야 원인 규명 가능
- 이 정의 없이 겹치면 "다르다"만 나오고 원인 불명.

### 3번 — STEP4 산출물 스펙 → **대체로 양호, `simplify(1.0)` 좌표계 확인 필수** ⚠️
- `score_grid`를 중심점+원점·간격으로 압축(3.09→0.22MB), 프론트 사각형 복원 방식 동의.
- **⚠️ `exclusion.geojson`의 `simplify(1.0)`을 어느 CRS에서 하는지 확인:**
  - 5186(미터) tolerance 1.0 = 1m → 정상
  - **4326(도) 1.0 = 약 111km → 도형 붕괴**
  - 현재 `gis_service.py`는 4326에서 tolerance `0.0005`(≈55m) 사용 → 단위 체계 상이.
  - **"5186에서 simplify → 4326으로 투영" 순서인지 명시 요청.**

### 4번 — PostGIS 이관 → **버전·인덱스 OK, 권한·테이블명이 실제 이슈**
- 버전/인덱스: 1장 즉답대로 문제없음.
- **권한:** 현재 슈퍼유저 `postgres` 뿐 → 이관 시 슈퍼유저 공유 위험. **AI 전용 role** 생성 권장(1계층 참조 읽기 + 자기 산출 테이블 쓰기로 제한).
- **⚠️ 이관 0순위 — 테이블 네이밍 불일치:** 기존 ORM/`gis_service.py`는 `cadastral_lands`·`restricted_zones` 참조, live DB엔 `candidate_lands`·`mv_restricted_zones`만 존재. **정답 테이블을 먼저 확정하지 않으면 grid/내접원 쿼리가 "없는 테이블" 에러.**

### 5번 — DB 적재 요청 → **계층 분리 설계 좋음, 2건 강력 지지 + 1건 우려**
- **강력 지지 ①: 통계청 vs 행자부 코드 경고(11170 용산구/구로구 충돌)**
  - 현재 스키마에 코드 체계 주석 전무. "생활인구 16개 중 12개만 매칭, 누락 4개에 1위 동" 사고의 직접 원인.
  - → 컬럼 주석에 `-- 통계청 행정구역분류코드(행자부 코드 아님)` **명시 반영 요청.** 저비용·고임팩트. **5번 최우선.**
- **강력 지지 ②: 지오코딩 결과 DB 캐시**(2,928행×0.3s≈15분)
  - 추가로 **실패(negative) 결과도 캐시**해 재실행 시 실패 주소 재호출 방지, **주소 원본 버전을 캐시 키**에 포함(주소 변경 시 무효화).
- **우려: 연속지적도 44,459필지 "온디맨드" 처리**
  - 요청 시점에 4.4만 필지 로드+투영+인덱싱 → 첫 응답 지연 큼.
  - → **"활성 지역 사전 적재(pre-load) + 신규 지역만 온디맨드"** 권장.
- **추가 확인: 지적도 CRS 폴백**
  - "prj 없으면 5186 가정"은 서울 전용일 땐 안전하나, 전국 확장 시 다른 원점(5185/5187/5188) 지역에서 오류.
  - → 확장 전 **"prj 없으면 에러/보류"**로 변경 권장.

---

## 3. 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | **5번: 통계청/행자부 코드 주석 명시** | 매칭 사고 기발생·주석 전무, 저비용·고임팩트 |
| 2 | **4번: 정답 테이블 네이밍 확정** (candidate vs cadastral) | 이관 자체가 여기서 막힘 |
| 3 | **4번: AI 전용 최소권한 role 생성** | 슈퍼유저 공유 = 보안·사고 위험 |
| 4 | **3번: `simplify` CRS 단위 확인** | 잘못되면 배제표출 도형 붕괴 |
| 5 | 1번(원본→스키마+샘플로 축소) · 2번(비교 키·오차 정의) | 방향 OK, 범위만 조정 |

---

## 4. 회신 시 확정/합의할 4가지 (요약)

1. **코드 체계 주석** — 스키마 컬럼에 통계청/행자부 구분 명시
2. **정답 테이블명** — grid/내접원 연산 대상 테이블 확정(candidate_lands 계열)
3. **최소권한 role** — AI 파이프라인 전용 계정 범위 합의
4. **simplify 좌표계** — 5186 simplify 후 4326 투영 순서 확인

**바로 회신 가능한 확답:** PostGIS 3.3.4 · 대상 GiST 인덱스 구비 → 함수 사용·인덱스 활용 문제없음, 업그레이드 불필요.
