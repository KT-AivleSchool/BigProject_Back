# DB 공유 방안 비교 보고서 (A · B · C안)

> **작성 목적:** 현재 로컬 도커에만 존재하는 공간 DB(PostGIS)를 팀원 전체가 사용할 수 있게 하는 방법을 비교하고, **가용 기간 1주일 / 비용 / 폐쇄망·보안 우려**를 반영해 권고안을 도출한다.
> **전제 기간:** 1주일 | **작성일:** 2026-07-29

---

## 1. 배경 — 지금 상태 (실측)

현재 DB는 **작성자 로컬 PC의 도커 볼륨(`omnisite_postgres_data`)에만** 존재하며, 팀원은 데이터를 쓸 수 없다.

| 항목 | 실측 결과 |
|---|---|
| 엔진 | PostgreSQL 15 + **PostGIS 3.3.4** + **pgvector 0.5.1** (도커 컨테이너 `omnisite-postgres-db`) |
| 적재된 핵심 데이터 | `candidate_lands` 6,524건 · `national_properties` 2,486건 · `dong_boundaries` 16건 (모두 geom 포함) |
| 비어 있는 테이블 | `smoking_areas`·`commercial_shops`·`cctv_locations`·`street_trash_bins` 등 다수 0건 |
| 공간 인덱스 | GIST 인덱스 33개 생성됨 |
| 백업 파일 | `omnisite_backup.sql` (약 19MB) 존재하나 **`.gitignore` 처리 → 로컬에만 있음** |
| 원본 CSV | `data_임시/**/data/*` 역시 **gitignore** (`.gitkeep`만 커밋) |
| 커밋된 재현 재료 | `schema.sql`, `schema_cleaned_data*.sql`, `load_*.py` (스키마·적재 스크립트만) |

### 짚어둘 리스크 (공유 전 정리 필요)
- **기본 비밀번호 `postgres:postgres`** — `docker-compose.yml`, `app/config.py` 기본값. 네트워크 공유 시 즉시 위험.
- **하드코딩된 `SECRET_KEY`** — `app/config.py`의 JWT 기본키가 코드에 노출.
- **스키마-ORM 불일치** — live DB는 `candidate_lands` 계열(gam2 파이프라인), ORM(`app/db/models/spatial.py`)·`gis_service.py`는 `cadastral_lands`·`restricted_zones`를 참조 → 일부 API가 현재 DB에 그대로 붙지 않음.

---

## 2. 세 방안 요약 비교

| 구분 | **A. 백업 파일 공유** | **B. compose initdb 자동화** | **C. 공용 원격 DB** |
|---|---|---|---|
| 핵심 방식 | `omnisite_backup.sql`을 팀원에게 전달 → 각자 `psql`로 복원 | 백업을 `/docker-entrypoint-initdb.d/`에 물려 `docker compose up`으로 자동 복원 | 내부망 서버 1대에 DB 두고 전원이 같은 `DATABASE_URL`로 접속 |
| 데이터 형태 | **각자 로컬 복사본** | **각자 로컬 복사본** | **단일 공유 실시간** |
| 코드 변경 | 없음 | compose 1줄 | .env 수정 + 서버 세팅 |
| 예상 소요 | **0.5~1일** | **0.5~1일** | **0.5일(DB) + α(호스트·승인)** |
| 추가 비용 | **0원** | **0원** | 호스트 장비/서버 비용 (상황별) |
| 폐쇄망 | ✅ 무관 (파일 전달만) | ✅ 무관 | △ **내부 서버면 가능, 클라우드 RDS는 불가** |
| 실시간 공유 | ❌ (복사본이라 각자 따로 놈) | ❌ | ✅ |
| 주요 리스크 | 각자 데이터가 갈라짐 | "첫 부팅만" 실행 함정 | 호스트 확보·네트워크 승인·보안 관리 |
| 관리 부담 | 낮음 | 낮음 | **중~높음** |

### 2-2. 개선·확장 방안 (D · E · H · I)

A·B·C는 "파일 복사 / 복사 자동화 / 실시간 공유"의 3축이며, 아래는 각 축을 개선한 변형이다.

| 구분 | **D. 데이터 구운 도커 이미지** | **E. Git LFS 백업 포함** | **H. 경량 시드 커밋** | **I. 코드로 재생성** |
|---|---|---|---|---|
| 핵심 방식 | 데이터가 적재된 상태로 이미지 빌드 → `docker save` tar로 전달 | `omnisite_backup.sql`을 Git LFS로 추적해 repo에 포함 | 핵심 수백 건만 뽑은 `seed.sql`을 git에 커밋 | `schema.sql`+`load_*.py`+원본 CSV로 재생성 |
| 팀원 작업 | `docker load` → `up` | `git clone` (LFS 자동) | `git clone` | `git clone` + 적재 스크립트 실행 |
| 데이터 범위 | 전체(풀셋) | 전체(풀셋) | **일부(개발/CI용)** | 전체 |
| 폐쇄망 | ✅ tar로 오프라인 전달 | △ LFS 지원 원격 필요 | ✅ | ✅ (CSV 확보 시) |
| "첫 부팅만" 함정 | ❌ 없음(이미지에 내장) | 해당 없음 | 해당 없음 | 해당 없음 |
| 비용 | 0원 | 0원(LFS 용량 내) | 0원 | 0원 |
| 주요 리스크 | 이미지 커짐·데이터 변경 시 재빌드 | 덤프 민감정보 노출 위험 | 풀셋은 별도 필요 | CSV 용량·민감정보·적재시간 |
| 적합 상황 | **폐쇄망+"clone하면 바로 실행"** | repo 단일화·민감정보 없음 | **테스트/CI 재현성** | 장기 재현성(IaC) |

---

## 3. 방안별 상세

### A안 — 백업 파일 공유
- **방식:** 작성자가 `omnisite_backup.sql`(19MB)을 git 외 경로(팀 스토리지·드라이브·USB)로 전달. 팀원은 `docker compose up -d db` 후 `psql -U postgres -d omnisite < omnisite_backup.sql`로 복원.
- **소요:** 0.5~1일 (파일 전달 + 각자 복원·검증).
- **비용:** 0원 (기존 로컬 도커 그대로).
- **폐쇄망:** 무관. 파일만 전달되면 인터넷 불필요.
- **우려:**
  - 복원 후에는 **각자 로컬 복사본** → 누가 데이터를 바꿔도 서로 반영 안 됨(개발용으론 오히려 안전).
  - 백업 안에 민감정보가 있으면 파일 관리 주의(그래서 gitignore된 것으로 추정).

### B안 — compose initdb 자동화
- **방식:** `docker-compose.yml`에 한 줄 추가.
  ```yaml
  db:
    volumes:
      - omnisite_postgres_data:/var/lib/postgresql/data
      - ./omnisite_backup.sql:/docker-entrypoint-initdb.d/01_init.sql
  ```
  팀원은 `docker compose up` 한 번으로 자동 복원.
- **소요:** 0.5~1일 (A와 유사, 백업 파일은 여전히 별도 공유 필요).
- **비용:** 0원.
- **폐쇄망:** 무관.
- **우려:**
  - **"첫 부팅 때만" 실행** — 볼륨에 데이터가 이미 있으면 무시됨. 백업 갱신 후 `up` 해도 반영 안 돼 `docker compose down -v` 필요 → 오히려 혼선.
  - A 대비 절약되는 건 "psql 명령 1줄"뿐이라 **소규모 팀엔 이득이 marginal**. 온보딩이 잦을 때 값을 함.

### C안 — 공용 원격 DB
- **방식:** 내부망 서버(또는 상시 가동 머신) 1대에 도커 DB를 올리고, 전원이 그 내부 IP로 접속.
  ```bash
  # 팀원 .env
  DATABASE_URL="postgresql://omnisite_app:강한비번@10.0.1.50:5432/omnisite"
  ```
- **소요:** DB 순수 작업 0.5일. **단 호스트 확보·네트워크 승인이 변수** (아래 4절).
- **비용:** 호스트 장비/서버 비용 발생 (5절).
- **폐쇄망:** **내부 서버면 가능**(외부 접근이 막혀 오히려 안전). **클라우드 RDS는 인터넷 필요 → 폐쇄망 불가.**
- **우려 (전환 전 필수 조치):**
  - 기본 비번 `postgres:postgres` → **강한 비번 교체**.
  - 5432 포트 `0.0.0.0` 노출 → **방화벽·`pg_hba.conf`로 팀 IP만 허용**, 인터넷 노출 금지.
  - 슈퍼유저 공유 금지 → **앱 전용 role 최소권한**(SELECT/INSERT/UPDATE/DELETE), 관리자만 슈퍼유저.
  - `SECRET_KEY` .env 분리·재발급.
  - 단일 DB → 한 번 깨지면 전원 영향 → **정기 `pg_dump` 백업** 필수.

### D안 — 데이터까지 구운 도커 이미지 (폐쇄망 최적)
- **방식:** 빈 DB 이미지가 아니라, **데이터가 이미 적재된 상태**로 이미지를 빌드한 뒤 파일 하나로 배포.
  ```dockerfile
  # Dockerfile.db
  FROM postgis/postgis:15-3.3
  RUN ... pgvector 0.5.1 설치 ...
  COPY omnisite_backup.sql /docker-entrypoint-initdb.d/01_init.sql
  ```
  ```bash
  # 작성자: 빌드 → tar 저장
  docker build -t omnisite-db:seeded -f Dockerfile.db .
  docker save omnisite-db:seeded -o omnisite-db.tar
  # 팀원: 오프라인 tar 불러와 즉시 실행
  docker load -i omnisite-db.tar && docker compose up -d
  ```
- **소요:** 0.5~1일 (이미지 빌드 + tar 전달 + 팀원 검증).
- **비용:** 0원.
- **폐쇄망:** ✅ `docker save/load`로 **인터넷·레지스트리 없이** 파일 전달만으로 배포.
- **B 대비 우위:** ① "첫 부팅만 실행" 함정 없음(데이터가 이미지에 내장) ② 팀원이 `psql`·별도 백업파일 불필요 → `docker load`+`up` 한 번 ③ 이미지 태그로 데이터 버전 관리 가능.
- **우려:** 이미지 용량이 데이터만큼 증가 / 데이터 갱신 시 이미지 재빌드·재배포 / 덤프 내 민감정보는 이미지에도 포함되므로 배포 대상 관리 필요.

### E안 — Git LFS로 백업 포함 (요약)
- `omnisite_backup.sql`(약 19MB)을 Git LFS로 추적 → `git clone`만으로 백업 동반. 단일 출처화.
- **제약:** LFS 지원 원격 필요(폐쇄망 자체 git 서버면 지원 여부 확인), 덤프에 민감정보 있으면 부적합.

### H안 — 경량 시드 데이터 커밋 (테스트/CI 보완)
- 전체 대신 핵심 수백 건만 뽑은 `seed.sql`을 git에 커밋 → clone만으로 즉시 개발·테스트 가능.
- **효과:** 현재 빈 DB로 도는 CI·신규 인원 온보딩의 재현성 확보. 용량·민감정보·gitignore 문제 회피.
- **한계:** 전체 데이터가 필요한 경우 A/D로 풀셋을 별도 확보. → **풀셋(D) + 경량(H) 조합 권장.**

### I안 — 코드로 재생성 (요약)
- `schema.sql`+`load_*.py`+원본 CSV로 DB를 빌드 산출물처럼 재생성. 가장 근본적 재현성.
- **제약:** CSV 용량·민감정보·적재 시간, 스키마-ORM 정합 선행 필요.

---

## 4. 1주일 일정 관점 평가

| 방안 | 1주 안에 완료 확실성 | 근거 |
|---|---|---|
| A | ★★★ 매우 높음 | 파일 전달 + 복원, 반나절이면 끝 |
| B | ★★★ 매우 높음 | compose 1줄, A와 동일 수준 |
| C | ★~★★ 조건부 | **DB 작업은 하루면 되나, 폐쇄망 포트 개방·서버 배정 승인이 며칠~주 단위로 늘어질 수 있음** |

**핵심:** C의 병목은 DB 작업이 아니라 **"24시간 켜둘 호스트 확보 + 폐쇄망 접근 승인"**. 이 둘이 이미 준비돼 있으면 C도 1주 내 여유, 하나라도 미확보면 1주를 초과할 위험.

### C안 진행 가정 시 1주 샘플 일정
- D1: 호스트 지정 + 네트워크/포트 승인 요청 (← 최대 불확실 구간)
- D2: 도커 DB 기동 + 백업 복원 + 확장 확인
- D3: 앱 전용 role·비번·`pg_hba` 보안 설정
- D4: 팀원 접속 테스트 + `.env` 배포
- D5: 정기 백업·장애 대응 정리 + 문서화
- (예비 D6~D7: 승인 지연·트러블슈팅 버퍼)

---

## 5. 비용 측면

| 방안 | 직접 비용 | 간접/관리 비용 |
|---|---|---|
| A | **0원** | 낮음 (각자 복원 1회) |
| B | **0원** | 낮음 (compose 1줄, 단 함정 안내 필요) |
| C (사내/교육망 유휴 서버 활용) | **0원~소액** | 중 (보안·백업·계정 관리 지속) |
| C (신규 장비/전용 서버 필요) | **장비·전력·운영 비용 발생** | 중~높음 |
| C (클라우드 RDS) | 월 사용료 발생 (**폐쇄망에선 사용 불가**) | 낮음(관리형)이나 망 조건 불충족 |

**요약:** A·B는 사실상 무비용. C는 "이미 있는 내부 서버를 재활용"하면 저비용이지만, **장비를 새로 확보하거나 클라우드를 쓰려면 비용·망 제약**이 붙는다. 폐쇄망 조건상 클라우드 RDS는 선택지에서 제외.

---

## 6. 권고

### 목적별 최적안

| 목적 | 최적안 |
|---|---|
| **폐쇄망 + "clone하면 바로 실행"** | **D (데이터 구운 이미지 + `docker save`)** — B의 상위호환 |
| 테스트/CI 재현성 + 가벼움 | **H (경량 시드 커밋)** |
| repo 단일화(민감정보 없음) | E (Git LFS) |
| 장기 재현성(코드로 생성) | I (CSV 공유 + load 스크립트) |
| 즉시·무비용·단순 | A (백업 파일 공유) |
| 실시간 공동 편집 | C (공용 원격 DB) |

### 결론: **D안(풀셋) + H안(경량 시드) 조합** — 필요 시 C안 전환

1. **개발용 풀셋 공유 → D안 채택 (권장).**
   - 폐쇄망에서 `docker save` tar 파일 하나로 배포, 팀원은 `docker load`+`up` 한 번.
   - B의 "첫 부팅만 실행" 함정 없음. 무비용·1주 내 충분(이미지 빌드 반나절).

2. **테스트/CI·신규 인원 → H안 병행.**
   - 경량 `seed.sql`을 git에 커밋해 clone만으로 즉시 구동. 현재 빈 DB로 도는 CI 재현성 확보.
   - D(풀셋) + H(경량)가 상호 보완.

3. **가장 단순히 지금 당장이면 → A안**도 유효(무비용·반나절). D 준비 전 임시로 사용 가능.

4. **B안은 보류.** compose 1줄로 가볍지만 "첫 부팅만" 함정 대비 이득이 작아 D로 대체 권장.

5. **C안은 "실시간 공동 DB가 반드시 필요"해질 때 전환.** 착수 전 ① 24시간 상시 호스트 ② 폐쇄망 5432 접근 승인 — 둘 다 확정돼야 1주 내 가능.

### 어느 안이든 공통 선결 과제
- [ ] 기본 비밀번호 `postgres:postgres` 교체
- [ ] `SECRET_KEY` .env 분리·재발급
- [ ] 스키마-ORM 불일치(`cadastral_lands` ↔ `candidate_lands`) 정합 — 공유 후 API 정상 동작 위해 필요

---

## 부록 A. 현재 DB 실측 데이터 (참고)

| 테이블 | 행수 | geom |
|---|---|---|
| candidate_lands | 6,524 | 6,524 |
| national_properties | 2,486 | 2,486 |
| dong_boundaries | 16 | 16 |
| smoking_areas / smoking_area_polygons | 0 | 0 |
| commercial_shops / cctv_locations / street_trash_bins | 0 | 0 |

- 확장: `postgis 3.3.4`, `vector 0.5.1` / GIST 공간 인덱스 33개

## 부록 B. C안 보안 체크리스트
1. `POSTGRES_PASSWORD` 강한 비번 교체 (compose + .env)
2. 앱 전용 role `omnisite_app` 최소권한 부여, 슈퍼유저 비번은 관리자만 보관
3. 방화벽 / `pg_hba.conf`로 팀 IP 대역만 5432 허용, 인터넷 노출 금지
4. `SECRET_KEY` .env 분리 후 `openssl rand -hex 32` 재발급
5. 정기 `pg_dump` 백업 스케줄
6. 접속 계정 분리로 접근 기록 관리
