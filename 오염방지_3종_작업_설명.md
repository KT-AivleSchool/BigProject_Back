# 오염 방지 3종 작업 — 필요성 및 구현 내용

> 대상 항목: 5. 승인 게이트 / 6. 룰 라이브러리 / 7b. `_get_fallback_data()` 확인
> 배경: 조례 RAG 업그레이드 발제문 #91(오염 방지), #90(캐싱) 대응

---

## 왜 세 가지가 다 필요한가 — 한눈에

이 시스템은 결국 "AI가 조례를 검색해서 참고하되, 실제 법적 판정(설치 가능/불가능)은 AI 검색이 아니라 확정된 기준으로 한다"는 원칙 위에 서 있습니다. 세 작업은 이 원칙을 지키기 위한 서로 다른 방어선입니다.

| # | 방어하는 문제 | 검증 대상 |
|---|---|---|
| 5. 승인 게이트 | 신뢰할 수 없는 출처의 문서가 검색에 섞이는 것 | 문서 **출처**가 믿을 만한가 |
| 6. 룰 라이브러리 | AI 검색 결과의 부정확한 숫자를 그대로 법적 판정에 쓰는 것 | 조문 안 **숫자**가 정확한가 |
| 7b. 폴백 확인 | 시스템 장애를 정상 응답처럼 위장하는 것 | 검색 실패 시 **정직하게 실패를 알리는가** |

---

## 5. 승인 게이트 (`ordinance_documents.status`)

### 왜 필요한가

지금까지는 우리(개발자)가 law.go.kr에서 직접 받은 공식 조례만 넣었기 때문에 문제가 없었습니다. 하지만 앞으로 **일반 유저가 조례 파일을 직접 업로드하는 기능**이 생기면 상황이 달라집니다.

- 검증 없이 아무 파일이나 바로 pgvector 검색 대상에 들어가면, 위조되거나 잘못된 문서가 실제 조례처럼 AI 토론에 인용될 수 있음
- 특정 지역 유저가 문서를 몰아서 올리면, AI가 "모든 기준이 이 지역 기준"이라고 착각하는 지역 편향 문제 발생 가능
- 옛날에 폐지된 조례가 최신 조례처럼 섞여 검색될 위험

### 어떻게 막는가 (설계)

`ordinance_documents` 테이블에 문서마다 상태를 매기고, **`approved`인 문서만 실제로 pgvector에 적재**되도록 합니다.

```sql
CREATE TABLE ordinance_documents (
    id SERIAL PRIMARY KEY,
    district_id INT REFERENCES districts(id),
    doc_title VARCHAR(250) NOT NULL,
    source_type VARCHAR(20) NOT NULL DEFAULT 'official_seed'
        CHECK (source_type IN ('official_seed','user_upload')),
    status VARCHAR(20) NOT NULL DEFAULT 'pending'
        CHECK (status IN ('pending','approved','rejected')),
    uploaded_by INT REFERENCES users(id),
    approved_by INT REFERENCES users(id),
    approved_at TIMESTAMP,
    ...
);
```

**판단 기준(무엇을 보고 승인/거부하는지)**은 세 가지입니다:
1. **출처 공신력** — law.go.kr, elis.go.kr 같은 정부 공식 사이트에서 나온 문서인지
2. **시행일 유효성** — 원문의 `[시행 yyyy.mm.dd]`, `<개정/삭제 ...>` 태그를 보고 지금도 유효한 조문인지
3. **업로더 신뢰도** — `official_seed`(개발자 직접 수집)는 자동 승인 가능, `user_upload`(유저 업로드)는 관리자가 위 두 가지를 직접 확인한 후에만 승인

### 현재 구현 상태

**미구현.** 코드 주석에만 존재를 언급하고 있고, 실제 `CREATE TABLE`은 아직 실행하지 않았습니다. 지금은 `official_seed`만 다루고 있어 당장 리스크는 낮지만, 유저 업로드 기능이 붙기 전에는 반드시 만들어야 합니다.

---

## 6. 룰 라이브러리 (`domain_regulation_rules`)

### 왜 필요한가

pgvector 검색은 **"관련 있어 보이는 조문을 찾아주는 것"** 이지, **"정확한 숫자를 보장하는 것"** 이 아닙니다.

- 같은 질문도 표현을 바꾸면 검색 결과 순위가 달라짐 → 판정 기준이 매번 흔들릴 위험
- AI가 조문 원문의 숫자를 잘못 인용하거나, 삭제된 조항의 숫자를 그대로 인용할 위험
- 지역구 조례에 없는 항목은 상위법(국민건강증진법 등)을 찾아 적용해야 하는데, 이건 법 해석 판단이 필요해 AI가 자동으로 처리하기엔 위험함

실제로 이번 검증 과정에서, 사전에 예시로 들었던 "학교 200m"라는 숫자가 근거 없는 임의값이었고, **원문을 실제로 대조해보니 정확한 값은 30m(국민건강증진법 제9조제6항제3호)** 였다는 것을 확인했습니다. 이게 바로 룰 라이브러리가 막아야 하는 "룰 오시딩(수치 오인용) 사고"의 실제 사례입니다.

### 어떻게 구현했는가 (실제 작업 내용)

**1) 테이블 생성**

```sql
CREATE TABLE domain_regulation_rules (
    id SERIAL PRIMARY KEY,
    district_id INT NOT NULL REFERENCES districts(id),
    domain VARCHAR(50) NOT NULL DEFAULT 'smoking_booth',
    facility_type VARCHAR(50) NOT NULL,
    rule_type VARCHAR(20) NOT NULL CHECK (rule_type IN ('exclusion','inclusion')),
    distance_m NUMERIC NOT NULL,
    legal_basis VARCHAR(250),
    source_document_id INT REFERENCES ordinance_documents(id) ON DELETE SET NULL,
    source_article_no VARCHAR(10),
    status VARCHAR(20) NOT NULL DEFAULT 'pending' CHECK (status IN ('pending','approved','rejected')),
    verified_by VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE (district_id, domain, facility_type, rule_type)
);
```

**2) pgvector로 관련 조문 검색 (참고용, 1차 후보 찾기)**

`asimilarity_search_with_relevance_scores()`를 임계치 필터 없이 직접 호출해서, 시설유형별 관련 조문 상위 5개를 확인했습니다. 예: `"버스정류소 이격 거리"`, `"학교 어린이집 이격 거리 금연구역"` 등으로 검색.

**3) 원문 전체를 사람이 직접 대조 (핵심 단계)**

검색 결과를 150자 미리보기가 아니라 **전문(全文)을 출력**해서, 실제 조항 번호와 숫자를 하나씩 확인했습니다. 이 과정에서:
- 용산구 조례 제5조제1항: 버스정류소·택시승차대 10m, 지하철역 출입구 10m 확인
- 국민건강증진법 제9조제6항: 유치원·어린이집·학교 각 30m 확인
- 서울시 조례에서 `<삭제 2026.1.5>` 태그가 붙은 조항(4호, 8호)은 더 이상 유효하지 않음을 확인하고 배제

**4) 검증된 값만 `status='approved'`로 입력**

```sql
INSERT INTO domain_regulation_rules
  (district_id, facility_type, rule_type, distance_m, legal_basis, source_article_no, status, verified_by)
VALUES
  (1, 'bus_stop', 'exclusion', 10, '서울특별시 용산구 금연구역 지정 및 간접흡연피해방지 조례 제5조제1항제3호', '5', 'approved', '규민'),
  (1, 'subway_exit', 'exclusion', 10, '서울특별시 용산구 금연구역 지정 및 간접흡연피해방지 조례 제5조제1항제4호', '5', 'approved', '규민'),
  (1, 'kindergarten', 'exclusion', 30, '국민건강증진법 제9조제6항제1호', '9', 'approved', '규민'),
  (1, 'childcare', 'exclusion', 30, '국민건강증진법 제9조제6항제2호', '9', 'approved', '규민'),
  (1, 'school', 'exclusion', 30, '국민건강증진법 제9조제6항제3호', '9', 'approved', '규민');
```

### 결과 — 확정된 규칙 5건

| facility_type | 거리 | 근거 |
|---|---|---|
| 버스정류소 | 10m | 용산구 조례 제5조제1항제3호 |
| 지하철역 출입구 | 10m | 용산구 조례 제5조제1항제4호 |
| 유치원 | 30m | 국민건강증진법 제9조제6항제1호 |
| 어린이집 | 30m | 국민건강증진법 제9조제6항제2호 |
| 학교 | 30m | 국민건강증진법 제9조제6항제3호 |

### 남은 것

- 도시공원, 교육환경보호구역(절대보호구역) — 거리(m)가 아니라 "그 구역 자체가 금연구역"인 케이스라 처리 방식 별도 논의 필요
- `candidate_lands`와의 공간 배제 쿼리(`ST_DWithin` + `NOT EXISTS`) 연동 — 다음 단계
- 기존 흡연구역 76곳과 대조해서 규칙이 현실과 맞는지 최종 검증 — 다음 단계

---

## 7b. `_get_fallback_data()` 확인

### 왜 확인이 필요했는가

과거 버전의 `retrieve_similar_statutes()`는, DB 연결 실패나 검색 오류가 나면 **하드코딩된 가짜 조문**을 반환하도록 되어 있었습니다:

```python
def _get_fallback_data(self) -> List[str]:
    return [
        "서울특별시 용산구 금연 환경 조성 조례 제4조: 주거지역 인근 10m 이내 금연구역 버퍼 지정",
        ...
    ]
```

이게 위험한 이유: 시스템이 실제로는 고장 나 있는데도, AI 페르소나 입장에서는 "정상적으로 조례를 찾았다"고 착각하고 그 가짜 문구를 진짜 조문처럼 인용해서 토론을 진행할 수 있습니다. **장애 상황이 정상 응답으로 위장되는 것**이라, 오염 방지 원칙에 정면으로 위배됩니다.

### 확인 결과

최신 `vector_db.py`(develop 브랜치 기준)를 검토한 결과, `_get_fallback_data()` 함수 자체가 더 이상 존재하지 않습니다. 대신:

```python
async def retrieve_similar_statutes(self, query, top_k=3, facility_type=None):
    if not self.statutes_store:
        return []          # 장애 시 빈 배열 반환 (가짜 데이터 아님)
    try:
        ...
        return filtered_docs
    except Exception as e:
        logger.error(f"[RAG Error] Vector DB Search Error: {e}")
        raise e             # 예외를 삼키지 않고 호출부로 전달
```

**결론: 이미 해결되어 있습니다.** DB 미연결 시 빈 배열(`[]`)을 반환하고, 검색 중 예외가 나면 그 예외를 그대로 호출부에 전달(`raise`)하도록 바뀌어서, 더 이상 장애가 정상 응답처럼 위장되지 않습니다. 별도 조치는 불필요하며, 확인 작업으로 종료합니다.

---

## 종합 상태

| 항목 | 상태 |
|---|---|
| 5. 승인 게이트 | 미구현 — 우선순위 낮음, 설계는 완료 |
| 6. 룰 라이브러리 | **구현 완료** — 그러나 결국 룰라이브러리도 업로드된 조례데이터 기반으로 찾음 |
| 7b. 폴백 확인 | **확인 완료** — 이미 해결된 상태, 추가 작업 불필요 |
