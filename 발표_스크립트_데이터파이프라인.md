# 조례 데이터 파이프라인 — 코드리뷰 발표 스크립트

> 발표자: 데이터팀 / 주제: 조례 원문을 RAG에 적재 가능한 형태로 전처리·적재하는 파이프라인
> 대상 파일: `app/core/data_pipeline/statute_parser.py`, `ingest_statutes.py`
> 사용법: 각 섹션 = [🖥️ 화면에 띄울 것] + [🎤 말할 내용] + [💡 핵심 포인트]

---

## 0. 발표 개요 (30초)

🎤 **말할 내용**
> "저는 데이터팀에서 **조례 원문을 AI가 검색·인용할 수 있는 형태로 가공해서 벡터 DB에 넣는 파이프라인**을 담당했습니다. 크게 두 부분입니다 — 원문을 **조문 단위로 잘라 구조를 입히는 파서**, 그리고 그 파서를 돌려 **실제로 적재하고 검증하는 스크립트**입니다. 역할로 보면 동현님이 만든 벡터 DB '엔진' 위에, 저희가 '연료(데이터) 품질'을 책임지는 전처리 계층을 얹은 구조입니다."

💡 한 문장 요약: **"조례 원문 → 조문 단위 청크 + 조례명 헤더 → 벡터 DB 적재"**

---

## 1. 문제 정의 — 왜 이 작업이 필요했나

🖥️ **화면**: 기존 방식 vs 개선 방식 비교

🎤 **말할 내용**
> "처음엔 조례 원문을 그냥 **1000자씩 기계적으로 잘라서** 넣었습니다. 그런데 이게 두 가지 문제를 만들었습니다."

| 문제 | 기존(1000자 통짜 절단) | 결과 |
|------|----------------------|------|
| ① 조문 반토막 | '학교 200m 이내 금지' 규정이 1000자 경계에서 잘림 | 검색해도 규정 절반만 나옴 |
| ② 출처 유실 | 청크에 조례명·조번호가 없음 | AI가 "무슨 조례 제 몇 조"인지 몰라 **근거 없이 답변**(환각) |

> "그래서 목표를 이렇게 잡았습니다 — **① 조문(條) 단위로 자르고, ② 각 청크 맨 앞에 `[조례명 > 제n조(제목) > 제n항]` 헤더를 박아서**, AI가 토론할 때 '용산구 금연조례 제4조에 따르면...' 하고 **출처를 인용**할 수 있게 만드는 것입니다."

💡 핵심: 이 발표의 모든 코드는 결국 **"검색 정밀도↑ + 인용 근거 확보"** 이 하나를 위한 것.

---

## 2. 전체 구조 (아키텍처)

🖥️ **화면**: 구조 다이어그램

```
┌─────────────────── 데이터팀 (제가 한 부분) ───────────────────┐
│                                                              │
│  ingest_statutes.py  ── 오케스트레이터 (무엇을 할지)         │
│    · 원문 파일 로드 (txt/pdf)                                 │
│    · parse_statute 호출  ────────┐                           │
│    · 적재 · 검증                  │ 위임                     │
│                                  ▼                           │
│  statute_parser.py   ── 파서 (어떻게 자를지)                 │
│    · 조/항 단위 절단 + 헤더 주입                             │
│                                  │                           │
└──────────────────────────────────┼──────────────────────────┘
                                   ▼  add_statute_chunks()
              ┌─────────── 동현님 엔진 ───────────┐
              │  vector_db.py  (PGVector)          │
              │  · 임베딩 · 저장 · 검색 · 격리     │
              └────────────────────────────────────┘
```

🎤 **말할 내용**
> "구조를 딱 두 파일로 나눴습니다. **`ingest_statutes.py`는 오케스트레이터** — '무엇을 할지' 순서를 지휘합니다. 파일 읽고, 파서 부르고, 적재하고, 검증하고. 그리고 **`statute_parser.py`는 파서** — '어떻게 자를지' 규칙을 소유합니다. 파싱 규칙을 바꾸려면 파서만 고치면 되고, 적재 순서를 바꾸려면 스크립트만 고치면 됩니다. **관심사를 분리**한 거죠."

💡 핵심: **"오케스트레이터(ingest) / 파서(parser) 분리"** — 한쪽을 고쳐도 다른 쪽에 영향 없음.

---

## 3. 데이터 흐름 (End-to-End)

🖥️ **화면**: 흐름도

```
조례 원문(.txt/.pdf)
   │
   ▼  ① _load_txt / _load_pdf        ── 파일에서 (제목, 본문) 추출
   ▼  ② extract_doc_meta            ── 시행일·조례번호 뽑기
   ▼  ③ resolve_facility_type       ── 이 조례가 어떤 시설용인지 태깅
   ▼  ④ parse_statute  ★핵심★       ── 조/항 단위 절단 + [조례명>조>항] 헤더 주입
   │        (StatuteChunk 리스트 반환)
   ▼  ⑤ _safe_clean_collection      ── 기존 적재분 정리 (재적재 규율)
   ▼  ⑥ add_statute_chunks          ── 임베딩 + 벡터 DB 적재
   ▼  ⑦ 검증 질의 (필터 O/X 이중)   ── 실제로 검색되는지 확인
```

🎤 **말할 내용**
> "전체 흐름은 7단계입니다. 파일을 읽어서 제목·본문·메타를 뽑고(①②③), **④번에서 파서가 조문 단위로 자르고 헤더를 붙입니다 — 여기가 핵심**입니다. 그다음 기존 데이터를 정리하고(⑤) 적재한 뒤(⑥), 마지막에 실제로 검색이 되는지 검증 질의를 돌립니다(⑦)."

💡 핵심: 파싱(④)이 심장, 나머지는 그걸 감싸는 전후 처리.

---

## 4. 핵심 코드 ① — `statute_parser.py` (파서)

### 4-1. `parse_statute()` — 조문 단위 절단 + 헤더 주입

🖥️ **화면**: [statute_parser.py:155-160](app/core/data_pipeline/statute_parser.py#L155-L160)

```python
starts = list(ARTICLE_START_RE.finditer(main))   # ① "제N조" 시작 위치 전부 찾기
for i, sm in enumerate(starts):
    start = sm.start()
    end = starts[i + 1].start() if i + 1 < len(starts) else len(main)
    art = main[start:end].strip()                 # ② 이 조 시작~다음 조 직전까지 잘라냄
```

🎤 **말할 내용**
> "이게 자르는 심장부입니다. **'제N조'로 시작하는 모든 위치에 책갈피를 다 꽂아두고(finditer), 책갈피와 다음 책갈피 사이를 한 조문으로 뜯어냅니다.** 마지막 조문은 문서 끝까지요."
>
> "왜 이렇게 했냐면 — 예전엔 '제1조'라는 **글자를 검색**해서 잘랐는데, 본문 안에 '제1조에 따라...' 같은 **인용 문구**가 있으면 거기로 잘못 점프했습니다. finditer는 줄 맨 앞 기준 위치를 좌표로 들고 있어서, 이런 오판이 원천적으로 사라집니다."

🖥️ **화면**: 항(項) 분할 + 헤더 주입 [statute_parser.py:185-203](app/core/data_pipeline/statute_parser.py#L185-L203)

```python
parts = CLAUSE_RE.split(body_text)
if len(body_text) > CLAUSE_SPLIT_THRESHOLD and len(parts) > 1:  # 500자 넘고 ①②③ 있으면
    pieces = [...]           # 항 단위로 더 잘게
else:
    pieces = [(None, body_text)]

for clause_no, piece in pieces:
    hdr = [doc_title, chapter, art_label]     # [조례명 > 제n장 > 제n조(제목)]
    if clause_no: hdr.append(f"제{clause_no}항")
    emit(hdr, piece, meta)                     # 헤더 + 본문을 한 청크로
```

🎤 **말할 내용**
> "조문이 너무 길고(500자 초과) 항 마커 ①②③가 있으면, **항 단위로 한 번 더** 쪼갭니다. 그리고 각 조각마다 `[조례명 > 장 > 조 > 항]` 헤더를 만들어 붙입니다. 이 헤더가 바로 나중에 AI가 인용할 **출처 꼬리표**입니다."

💡 핵심 포인트 3개:
- **finditer 오프셋 순회** → 인용 문구에 안 속고 정확히 조문만 절단
- **적응형 분할** → 짧은 조는 통째로, 긴 조는 항 단위로 (검색 정밀도)
- **헤더 주입** → 청크 자체에 출처가 박혀 인용 근거 확보 (환각 억제)

### 4-2. `extract_doc_meta()` — 문서 메타 추출

🖥️ **화면**: [statute_parser.py:50-73](app/core/data_pipeline/statute_parser.py#L50-L73)

🎤 **말할 내용**
> "원문 앞머리의 `[시행 2014.12.26.] [조례 제1081호]` 같은 표기에서 **시행일과 조례번호**를 정규식으로 뽑습니다. 조례가 개정됐을 때 '어느 판(版)의 조문인지' 식별하려고요. 노이즈 줄(법제처 꼬리말 등)은 건너뛰고 첫 유효 줄에서 조례명도 탐지합니다."

### 4-3. 보조 함수들 (빠르게)

🎤 **말할 내용**
> "나머지는 원문의 지저분함을 걷어내는 청소 함수들입니다."

| 함수 | 역할 |
|------|------|
| `_strip_noise` | 페이지마다 반복되는 '법제처/국가법령정보센터' 머리말, 페이지 번호 제거 |
| `_split_addenda` | 본문과 '부칙'을 분리 (부칙이 본문 조문에 섞이는 것 방지) |
| `_extract_amend_tags` | `<개정 2020.1.1>` 같은 개정 태그를 본문에서 떼어 메타로 보존 (날짜가 임베딩 오염시키는 것 방지) |
| `_chapter_of` | 각 조문이 몇 장(章)에 속하는지 역추적 → 헤더에 사용 |
| `emit` | 헤더 + 본문을 합쳐 최종 `StatuteChunk` 하나로 배출 |

---

## 5. 핵심 코드 ② — `ingest_statutes.py` (적재 스크립트)

### 5-1. `load_seeds` / `_load_pdf` / `_load_txt` — 원문 로드

🖥️ **화면**: [ingest_statutes.py:77-94](ingest_statutes.py#L77-L94)

🎤 **말할 내용**
> "`seeds/` 폴더의 txt·pdf를 읽습니다. **PDF가 까다로웠는데**, law.go.kr PDF는 페이지마다 '법제처... 국가법령정보센터' 머리글이 반복되고, 조례명이 어디 있는지 일정치 않습니다. 그래서 앞 5줄을 훑어 머리글·조문 줄을 건너뛰고 '조례/규칙/법'이 든 줄을 제목으로 잡습니다."

### 5-2. `resolve_facility_type()` — 시설 태깅

🖥️ **화면**: [ingest_statutes.py:97-106](ingest_statutes.py#L97-L106)

🎤 **말할 내용**
> "이게 검색 필터의 핵심입니다. 문서마다 '이건 흡연부스용 조례', '이건 전기차충전소용'이라고 **facility_type을 태깅**합니다. 파일명이나 조례명에 '금연'이 들어가면 '흡연부스', '전기차'가 들어가면 '전기차충전소' 식으로요."
>
> "⚠️ 여기서 제일 조심한 건 — 이 태그 값이 **검색할 때 필터 값과 글자 하나까지 정확히 일치**해야 한다는 겁니다. 한 글자만 달라도 검색이 0건이 나거든요. 그래서 전 브랜치를 뒤져서 프론트가 보내는 값('흡연부스')을 확인하고 맞췄습니다."

💡 핵심: **적재 태그 = 검색 필터 값**. 이 계약이 깨지면 서비스에서 조용히 0건.

### 5-3. `_safe_clean_collection()` — 재적재 규율 (버그 수정 스토리)

🖥️ **화면**: [ingest_statutes.py:126-158](ingest_statutes.py#L126-L158)

🎤 **말할 내용**
> "이 함수는 **실제로 버그를 잡으면서 만든 것**입니다. 처음엔 재적재 전에 기존 데이터를 지우려고 라이브러리가 주는 `delete_collection()`을 썼는데, 이게 데이터뿐 아니라 **콜렉션 등록 정보 자체를 지워버려서**, 다음 적재 때 'Collection not found'로 전량 실패했습니다. 2번 연속 재현됐고요."
>
> "그래서 콜렉션 껍데기는 남기고 **안의 임베딩 행만 골라 지우는** SQL로 직접 교체했습니다."

💡 핵심: 라이브러리 기본 동작의 함정을 실측으로 발견 → 안전한 대체 구현.

### 5-4. `main()` — 겸용 호출 + 이중 검증

🖥️ **화면**: [ingest_statutes.py:205-244](ingest_statutes.py#L205-L244)

🎤 **말할 내용**
> "적재할 때 `metadatas` 인자를 지원하는 최신 버전이면 메타까지 넣고, 구버전이면 텍스트만 넣도록 **시그니처를 런타임에 확인해서 겸용**으로 호출합니다 — 동현님 코드 머지 타이밍과 무관하게 돌아가게요."
>
> "그리고 검증을 **반드시 두 번** 돌립니다. **필터 없이** 한 번(적재·임베딩 자체가 됐는지), **필터 걸고** 한 번(실제 서비스 경로 재현). 실서비스는 항상 facility_type 필터를 걸기 때문에, 필터 없이만 검증하면 태그가 틀려도 초록불이 뜨고 **서비스에서만 0건**이 나는 함정이 있거든요."

💡 핵심: **"필터 없이/걸고 이중 검증"** — 태그 불일치를 배포 전에 잡는 안전판.

---

## 6. 트러블슈팅 하이라이트 (발표 임팩트 구간)

🎤 **말할 내용**
> "실제 원문을 넣어보며 잡은 문제들이 이 파이프라인을 견고하게 만들었습니다."

| # | 문제 | 원인 | 해결 |
|---|------|------|------|
| A1 | 앞머리 비슷한 조문 오판 | `find()` 텍스트 재검색이 본문 인용에 걸림 | **finditer 오프셋 순회**로 교체 |
| A6 | 특정 조례 청크 0개 | 조문이 공백 1칸 들여쓰기됨 (`^제\d+조`가 매치 실패) | 줄머리 선행 공백 허용 정규식 |
| — | PDF 조문 앞뒤 뒤섞임 | PDF 내부 저장 순서로 읽어 조판이 깨짐 | `get_text(sort=True)` 좌표 정렬 |
| — | 재적재 전량 실패 | `delete_collection()`이 콜렉션 등록까지 삭제 | `_safe_clean_collection` SQL |
| A3 | '제n조 삭제' 조항이 청크화 | 빈 껍데기 조문 | 삭제 조항 스킵 |

🎤 마무리 한마디
> "핵심은 — **표준 포맷을 가정하고 짜면 실원문에서 반드시 깨진다**는 걸 배웠고, 조판 편차·들여쓰기·페이지 노이즈 같은 현실을 하나씩 방어했습니다."

---

## 7. 결과 & 검증

🖥️ **화면**: 검증 결과

🎤 **말할 내용**
> "최종적으로 **조례 6종을 222개 청크로** 적재했고, 검증 질의에서 조례명·조번호가 헤더로 자동 포함돼 나오는 걸 확인했습니다. 유사도 임계치도 처음엔 감으로 0.25를 뒀다가, **실제 적재분에서 관련/무관 쌍의 점수 분포를 측정**해서 0.36으로 근거 있게 튜닝했습니다 — 관련 조문을 하나도 안 놓치는 최댓값으로요."

💡 핵심 숫자: **6종 · 222청크 · 임계치 0.36(실측)**

---

## 8. 예상 질문 대비 (Q&A)

**Q. 왜 LangChain 기본 splitter를 안 쓰고 직접 파서를 짰나요?**
> A. 기본 splitter는 글자 수로만 잘라서 조문을 반토막 내고, 출처 정보가 사라집니다. 법률 문서는 '조'가 의미 단위라, 조 단위로 자르고 헤더를 붙여야 검색 정밀도와 인용 근거가 확보됩니다. splitter는 초장문 세이프티넷으로만 남겨뒀습니다.

**Q. 사용자가 이상한 문서를 올리면요?**
> A. 현재 시드 경로는 표준 조례를 가정합니다. 실시간 업로드로 확장할 땐 parse_statute를 1차로 쓰되, 청크 0개가 나오면 splitter로 폴백하는 2단 구조가 필요합니다. (다음 작업)

**Q. facility_type을 왜 검색어에 안 섞고 메타로 분리했나요?**
> A. 검색어에 '흡연부스'를 붙이면 질의 임베딩이 시설명 쪽으로 왜곡됩니다. 조례 본문엔 시설명 토큰이 별로 없어서 유사도만 떨어지거든요. 그래서 의미 검색은 순수 질의로 하고, 시설 구분은 메타데이터 필터로만 겁니다.

---

## 발표 팁

- **시간 배분**(10분 기준): 개요·문제 2분 / 구조·흐름 2분 / 핵심 코드 3분 / 트러블슈팅 2분 / 결과·Q&A 1분
- **강조할 3가지**: ① 조 단위+헤더 주입의 *이유*, ② finditer 오판 제거, ③ 이중 검증·재적재 버그 수정 (엔지니어링 성숙도)
- 코드는 **전부 읽지 말고** 핵심 4~5줄만 띄우고 "무엇을/왜"로 설명
