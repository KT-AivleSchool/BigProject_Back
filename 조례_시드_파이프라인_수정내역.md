# 조례 시드 적재 파이프라인 수정 내역

> 작성: 2026-07-22 / 대상 커밋: `92a3265`(규민님) 기준 후속 수정
> 관련: 발제 `벡터DB_업그레이드_발제_v3.md`, PR #100(동현님 RAG 업그레이드)

## 요약

규민님이 올린 조례 시드 파이프라인(`ingest_statutes.py`, `statute_parser.py`)을 실원문으로 검증하고
발견된 3건을 수정했습니다. **파싱 로직 자체는 손대지 않았습니다** — 품질이 좋아서 배선·검증·CI만 정리했습니다.

수정 파일: `ingest_statutes.py`, `app/core/data_pipeline/statute_parser.py` (+ CI 복구용 3개 데이터 스크립트)

---

## 검증 결과 (수정 전, 실원문 기준)

시드: `seeds/서울특별시 용산구 금연구역 지정 및 간접흡연피해방지 조례(제1081호)(20141226).pdf`

```
청크 15개 | 최소 74 / 중앙값 174 / 최대 507자
1000자 초과: 0개 → splitter 미발동 확인
```

**발제 A-1의 "조 단위 텍스트는 1000자 미만이라 splitter 재분할 미발동" 주장이 실원문으로 실증됐습니다.**
최대 507자로 1000자 기준에 여유가 큽니다. 이슈에 실측치로 기입 가능합니다.

파서 고급 기능도 모두 정상 동작을 확인했습니다.

| 기능 | 결과 |
|---|---|
| 조 단위 분할 | 제1~11조 + 제9조의2 + 부칙 = 15청크 |
| 항 분할 | 제8조(500자 초과)만 ①②③ 3개로 분할 |
| `제n조의m` 본조 병기 | `제9조의2 — 본조: 제9조(금연교육 및 홍보지원)` |
| 개정 태그 분리 | `9조의2 → amended: '신설 2014.12.26'` (본문 제거, 메타 보존) |
| 노이즈 제거 | 법제처/국가법령정보센터 머리글 잔여 **0건** |
| 문서 메타 추출 | 시행일 `2014-12-26`, 조례번호 `제1081호`, 제목 자동 감지 |

---

## 수정 1. `extract_doc_meta()` 배선 (발제 B-4)

### 문제

`statute_parser.py`에 `extract_doc_meta()`가 구현돼 있는데 **아무도 호출하지 않았습니다.**
`ingest_statutes.py`는 `parse_statute, length_report`만 import 했습니다.

또 `parse_statute()`가 `document_id` / `district_id`를 키워드 인자로 받는데 ingest가 넘기지 않아,
`None`이면 메타에서 제외되는 로직 때문에 **전 청크에서 누락**됐습니다.

```
document_id 있는 청크: 0
district_id 있는 청크: 0
```

발제 B-4의 목적이 "조례 개정 시 **문서 단위** 삭제·재적재"인데, 문서를 식별할 키가 하나도 없으면
여전히 콜렉션 전체 재적재밖에 못 합니다. 시드가 1건이라 지금은 티가 안 나지만 2건째부터 막힙니다.

### 수정

**`statute_parser.py`** — `parse_statute()`에 `doc_meta` 파라미터 추가

```python
def parse_statute(
    text, doc_title, *,
    facility_type="흡연부스",
    document_id=None,
    district_id=None,
    doc_meta=None,          # ← 추가
):
```

`base_meta()`에서 `doc_meta`의 키를 전 청크 메타데이터에 병합합니다
(`doc_title_detected`는 `document_title`과 중복이라 제외).

**`ingest_statutes.py`** — `extract_doc_meta()` 호출 후 전달

```python
doc_meta = extract_doc_meta(text)
chunks = parse_statute(text, title, facility_type=SEED_FACILITY_TYPE, doc_meta=doc_meta)
```

### 결과

```
메타 키: ['article_no', 'article_title', 'chapter', 'clause_no',
          'doc_no', 'document_title', 'enforcement_date', 'facility_type', 'source']
샘플: {'facility_type': '흡연부스', 'enforcement_date': '2014-12-26', 'doc_no': '제1081호'}
```

`enforcement_date` + `doc_no` 조합으로 **"어느 판(版)의 조문인지" 식별이 가능**해졌습니다.
조례 개정 시 이 값으로 구판을 걸러내 삭제할 수 있습니다.

### 남은 것

`document_id`(정수 PK)는 발제 #5의 `ordinance_documents` 테이블이 아직 없어서 **의도적으로 비워뒀습니다.**
테이블이 생기면 INSERT 후 받은 PK를 `parse_statute(document_id=...)`로 넘기면 됩니다.
`extract_doc_meta()`가 뽑는 시행일·조례번호가 그 테이블의 INSERT 재료입니다.

`district_id`도 자치구 코드 체계가 확정되면 같은 방식으로 넘깁니다.

---

## 수정 2. 검증 질의에 필터 적용

### 문제

적재 후 검증 질의가 `retrieve_similar_statutes(q, top_k=3)` — **`facility_type`을 넘기지 않았습니다.**

그런데 실제 서비스([`simulations.py`](app/api/v1/simulations.py))는 **항상** 필터를 겁니다.
즉 적재 태그와 검색 필터 값이 어긋나 있어도 **검증은 초록불, 서비스만 0건**이 되는 구조였습니다.
가장 잡기 어려운 종류의 버그입니다.

### 수정

검증을 **두 벌** 돌리도록 바꿨습니다.

1. **필터 없음** — 적재·임베딩 자체가 정상인지 확인
2. **필터 적용** (`facility_type=SEED_FACILITY_TYPE`) — 실제 서비스 경로 재현

필터 적용 질의가 **전부 0건이면 경고를 출력**합니다.

```python
if empty == len(TEST_QUERIES):
    print("⚠️ 필터 적용 시 전 질의 0건 — 적재 태그와 검색 필터 값이 어긋났을 가능성이 큽니다.")
```

두 벌의 결과가 갈리면 그 자체가 태그 불일치의 증거입니다.

---

## 수정 3. CI 복구

`ruff check` / `ruff format --check` 둘 다 실패하던 상태였습니다. (`.github/workflows/ci.yml`의 필수 단계)

| 항목 | 내용 |
|---|---|
| `E741` 2건 | `statute_parser.py`의 모호한 변수명 `l` → `line`, `n` |
| `F841` 1건 | `show_map.py`의 미사용 변수 `except Exception as e` → `except Exception` |
| 포맷 미적용 | `statute_parser.py`, `ingest_statutes.py`, `load_data.py`, `load_cleaned_data.py`, `show_map.py` |

### 주석 사본 제거 (중요)

두 파일 모두 **구버전 전체가 `#`로 주석 처리된 채 파일 앞부분에 남아 있었습니다.**

| 파일 | 수정 전 | 수정 후 | 제거 |
|---|---|---|---|
| `statute_parser.py` | 350줄 | 206줄 | 앞 144줄 (v1 사본) |
| `ingest_statutes.py` | 269줄 | 153줄 | 앞 116줄 (구버전 사본) |

`ingest_statutes.py`는 268줄 중 150줄이 주석/빈줄이었습니다.
새 버전을 앞에 덧붙이는 방식 대신 **기존 파일을 교체**해 주세요 — git이 이력을 갖고 있어
구버전을 파일에 남길 이유가 없고, 실제 코드가 어디부터인지 찾기 어려워집니다.

### 검증

```
ruff check   → All checks passed!
ruff format  → 43 files already formatted
pytest       → 1 passed
```

---

## ⚠️ 미해결 — `facility_type` 값 확정 필요 (팀 합의 사항)

**이것만은 코드로 해결할 수 없습니다. 값을 정해주셔야 합니다.**

### 문제

`facility_type`은 **정확일치(exact match) 필터**입니다. 한 글자만 달라도 검색 결과가 전량 0건이 됩니다.

```python
# vector_db.py
search_kwargs["filter"] = {"facility_type": facility_type}
```

그런데 현재 프로젝트 안에서 이 값이 **세 군데 서로 다릅니다.**

| 위치 | 값 |
|---|---|
| `statute_parser.py` 기본값 (적재 태그) | `"흡연부스"` |
| `조례데이터.csv` 시설종류 컬럼 | `"스마트흡연부스"` |
| `app/schemas/simulations.py` | `facility_type: str` — 자유 문자열, 설명만 `"예: 흡연부스"` |

스키마가 `str` 자유입력이라 **프론트엔드가 실제로 보내는 문자열이 유일한 정답**입니다.

### 확인해야 할 것

**장천명님(풀스택)께**: 프론트엔드가 `POST /simulations/stream`의 `facility_type`에
실제로 담아 보내는 문자열이 정확히 무엇인가요?

### 확정 후 반영 방법

`ingest_statutes.py` 상단 상수 **한 줄만** 바꾸면 적재·검증에 동시 반영되도록 정리해뒀습니다.

```python
# ⚠️ 적재 태그 = 검색 필터 값. 프론트가 보내는 facility_type과 '정확히' 일치해야 한다.
SEED_FACILITY_TYPE = "흡연부스"      # ← 이 줄만 교체
```

### 권장 (별건)

근본적으로는 `facility_type`을 `str` 대신 **`Enum`으로 좁히는 것**이 맞습니다.
지금 구조는 오타 하나로 RAG 전체가 조용히 무력화되는데, 예외도 안 나고 로그도 안 남습니다.
다만 프론트 API 계약 변경이라 이번 범위 밖으로 두고 별도 이슈로 제안합니다.

---

## 다음 단계

- [ ] `facility_type` 값 확정 → `SEED_FACILITY_TYPE` 교체 (**프론트 확인 대기**)
- [ ] 실적재 실행 (`python ingest_statutes.py`) — 3-small 재적재 겸함
- [ ] 필터 적용 검증 질의 통과 확인 (0건 경고가 안 뜨는지)
- [ ] 유사도 임계치 실측 → `vector_db.py`의 `SIMILARITY_THRESHOLD = 0.25` 조정 (발제 A-3)
      · 현재 0.25는 ada-002 시절 경험치이고 3-small로 바뀌어 스케일이 달라진 상태 — 근거 없음
      · 무관 질의(예: "전기차 충전시설 설치 의무")의 점수 분포를 보고 결정
- [ ] 시드 확대: 서울시 금연환경 조성 조례, 국민건강증진법, 교육환경법 (발제 67행)
      · 대조군(EV 조례)이 들어와야 `facility_type` 필터 효과를 실증할 수 있음
- [ ] `ordinance_documents` 테이블(발제 #5) 신설 → `document_id` 배선 완료
