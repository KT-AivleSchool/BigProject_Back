name: Deploy Backend to EC2

on:
  push:
    branches:
      - main
      - main2
      - develop
      - develop2
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to EC2 using SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/BigProject_Back
            
            # 1. Fetch the exact triggered branch code
            git fetch origin ${{ github.ref_name }}
            git checkout ${{ github.ref_name }}
            git reset --hard origin/${{ github.ref_name }}
            
            # 2. Build and restart containers (runs detached)
            # The .env file must be created manually on the EC2 instance in ~/BigProject_Back/.env
            docker-compose up -d --build
            
            # 3. Clean up dangling images to save space
            docker image prune -f
